import a from '../../component/a';
import component from '../../component/component';
import isJustineAnAdult from '../../ts/a';

document.body.appendChild(
  component(['Hello', 'webpack', a.a, isJustineAnAdult])
);
